import pandas as pd
from scipy import stats
import math
import numpy as np

def extract_features(df: pd.DataFrame,  features_columns_range, target) -> pd.DataFrame:

    dataset = df.copy()

    def preprocess(dataset):
            '''
			Your code is here !!!
			'''
        
        return data
    
    dataset = preprocess(dataset)

    return dataset